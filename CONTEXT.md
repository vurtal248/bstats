## Purpose
Provides the semantic HTML skeleton, resource inclusion mapping, and repository documentation.

## Public API
Internal only.

## Constraints
MUST load JavaScript securely as ES modules.

## Out of Scope
Application logic and client-side data storage (owned by `js/`); visual styling rules and animations (owned by `css/`).
